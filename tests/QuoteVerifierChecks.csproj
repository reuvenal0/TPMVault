<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <!-- Build the actual project in the selected configuration; checks never access TPM hardware. -->
  <ItemGroup>
    <ProjectReference Include="../TPMVault.csproj" />
  </ItemGroup>
</Project>
